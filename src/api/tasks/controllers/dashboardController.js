export function getDashboard(req, res, next) {
  res.status(200).json("Dashboard route is working!");
}

export function deleteDashboard(req, res, next) {
  res.status(200).json("Hello world!");
}
