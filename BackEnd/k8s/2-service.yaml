apiVersion: v1
kind: Service
metadata:
  name: scc-service
  labels:
    app: scc
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 5173
      protocol: TCP
      targetPort: 5173
  selector:
    app: scc
